package Common;

/**
 * Enum made to define different commands
 */
public enum Command{
    LOGIN,
    ADD_OFFER,
    REMOVE_OFFER,
    GET_OFFERS,
    ADD_ASSET,
    REMOVE_ASSET,
    GET_ASSET,
    CHANGE_PASSWORD,
    FAIL,
    SUCCESS
}